/* Question: 3
1 - String
2.NAN means Not A Number
Number , Math operation .....
if(isNaN(inputValue) == true)
3 - to monitor the Call Stack and the Callback Queue
4 - Polymorphism in Java is the 
ability of an object to take many forms. 
To simply put, polymorphism in java allows us 
to perform the same action in many different ways. 
Any Java object that can pass more than one 
IS-A test is considered to be polymorphic and in 
java, all the java objects are polymorphic as it 
has passed the IS-A test for their own type and
for the class Object.